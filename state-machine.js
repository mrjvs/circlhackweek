class StateMachine {
    
    // set a state
    setState(userID, key, value) {

    }

    // get a state, returns it
    getState(userID, key) {

    }
    
    // clears a state
    clearState(userID, key) {

    }

    // clear a user's states
    clearUserStates() {

    }

    // list all possible states, returns object with user states
    listUserStates(userID) {
        
    }
}

// make and export instance
const INS = StateMachine();
module.exports = INS;